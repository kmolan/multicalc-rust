# The packages the generators use directly. Edit this file, then rebuild
# requirements.lock — the lock is what gets installed, and it also pins
# everything these four pull in.

numpy==2.1.3
scipy==1.14.1
mpmath==1.3.0
filterpy==1.4.5

# Added in later phases:
# pin==...
# mujoco==...
