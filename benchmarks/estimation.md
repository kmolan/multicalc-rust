# Estimation accuracy

_Table below generated by `cargo run -p multicalc-qa --bin gen_accuracy_tables`; do not edit it by hand._

Every row runs a fixed measurement sequence through predict/update and compares the final estimate
against [FilterPy](https://github.com/rlabbe/filterpy), bar the last four: numpy and scipy instead.

In the equations, `x` is the state vector, `u` the control input, and `x⁻` the predicted state.
`F` is the state-transition matrix (`x⁻ = F·x`), `H` the measurement model mapping state to
measurement, and `h` its nonlinear counterpart (`z = h(x)`). `f` is the matching counterpart on the
prediction side (`x⁻ = f(x)`), used where the motion itself curves: the coordinated-turn rows carry
the state `[x, y, θ, v, ω]` (position, heading, forward speed, and turn rate) one tick along a
turning arc. `α` is the unscented filter's spread setting.

The last two rows are attitude-only filters: `R` is the body's facing, `w` the turn-rate reading,
`b` the turn-rate sensor's steady offset, and `e` the small turn that would bring the estimated
facing onto what the push sensor and the magnetometer say. `kP` and `kI` are the Mahony filter's
two gains; `beta` and `zeta` are the Madgwick filter's.

<!-- BEGIN generated: accuracy -->
| Operation | Equation | Tolerance | Tested Against |
| --- | --- | --- | --- |
| Linear Kalman filter, 8 steps, state 2 / measurement 1 | F = [[1, 1], [0, 1]], H = [1, 0] | 1e-9 | FilterPy 1.4.5 |
| Linear Kalman filter with control, 8 steps, control 1 | x⁻ = [[1, 1], [0, 1]]·x + [[0.5], [1]]·u, H = [1, 0] | 1e-9 | FilterPy 1.4.5 |
| Extended Kalman filter, 8 steps, state 5 / measurement 2 | f = coordinated turn on [x, y, θ, v, ω], h = [x, y] | 1e-9 | FilterPy 1.4.5 |
| Unscented Kalman filter, 8 steps, state 5 / measurement 2, spread 1e-3 | f = coordinated turn on [x, y, θ, v, ω], h = [x, y], α = 1e-3 | 1e-7 | FilterPy 1.4.5 |
| Error-state Kalman filter, 200 IMU steps + 20 position fixes + 10 heading aids, error state 15 / measurement 3 and 1 | p- = p + v*dt + 0.5*(R*a + g)*dt^2, q- = q x exp(w*dt), reset G = I - 0.5*[dtheta]x | 1e-8 | in-house numpy transcription of Sola's error-state equations |
| Attitude from two direction pairs (down + north), noiseless | R = [r1 r2 r3] * [o1 o2 o3]^T from two direction pairs | 1e-12 | SciPy 1.14.1 |
| Mahony attitude filter, 400 steps, turn-rate + push + field readings, facing and turn-rate offset | e = sum(m_hat x R^T r_hat), b- = b - kI*e*dt, w_c = w - b + kP*e, R- = R x exp(w_c*dt) | 1e-8 | in-house numpy transcription of the Mahony and Madgwick attitude filters |
| Madgwick attitude filter, 400 steps, turn-rate + push + field readings, facing and turn-rate offset | e = sum(m_hat x R^T r_hat), b- = b - zeta*e_hat*dt, w_c = w - b + beta*e_hat, R- = R x exp(w_c*dt) | 1e-8 | in-house numpy transcription of the Mahony and Madgwick attitude filters |
<!-- END generated -->
